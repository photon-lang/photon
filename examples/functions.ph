fn add(x: i32, y: i32) -> i32 {
    let sum = x + y
}

fn multiply(a: i32, b: i32) -> i32 {
    let product = a * b
}

fn main() {
    let x = 10
    let y = 20
    let sum_result = add(x, y)
    let mult_result = multiply(x, y)
}